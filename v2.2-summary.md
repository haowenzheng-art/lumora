# v2.2 产品级打磨 · 总结

> **发布日期**：2026-07-03
> **commit**：`89bfaf6`（待 push，等 GitHub 网络恢复后补充）
> **上一版本**：v2.1.0（Edge TTS 免费路径）
> **测试**：12/12 memory_smoke 通过，flutter analyze 通过（剩 2 个 pre-existing warning）

---

## 一句话总结

v2.2 不是"加新特性"，是把 v2.1 已有的"能用"特性做到"产品级"——字体 / 触摸 / 转场 / 打字机 / 三态 / 破冰，6 个维度同步打磨，组成完整的"产品级第一印象"。

---

## 6 个改动一览

| 改动 | 工程量 | 核心文件 | 一句话效果 |
|---|---|---|---|
| A 字体 | 0.5 天 | `lib/theme.dart`（新建）| SplashPage logo + 口号从系统默认变思源宋体，"信札感"立住 |
| B 触摸 | 0.5 天 | `lib/widgets/pressable.dart`（新建）| 5 个关键按钮按下缩放 0.96，"产品手感"立刻有 |
| C 转场 | 0.5 天 | `lib/widgets/fade_scale_route.dart`（新建）| 8 处路由从平台默认左滑变淡入 + 微缩放，"沉静"调性 |
| D 打字机 | 1 天 | `lib/main.dart`（_MessageBubble + ChatPage）| 精灵消息逐字 + 闪烁光标，"在说话"实时感 |
| E 三态 | 1 天 | `lib/widgets/empty_state.dart` + `skeleton_box.dart`（新建）| Splash 空状态有引导文案，生成等待变 shimmer 占位 |
| H 破冰 | 2 天 | `lib/main.dart`（SpiritScenePage）| 立绘淡入 + 自动进入 ChatPage，"刚醒过来"叙事连贯 |
| **合计** | **5.5 天** | | |

---

## 为什么是"产品级打磨"而不是"加新特性"

v2.1 时用户感觉"还太过 demo"，但**Demo 感不是"少功能"**——是"细节不到位"。一个产品级应用跟 demo 应用的区别，往往不在于"加了多少按钮"，而在于：

- 字体是否经过挑选（vs 系统默认）
- 按下按钮有没有反馈（vs 点了没动静）
- 切换页面是平台默认左滑（vs 自定义转场）
- 消息是一次性显示（vs 逐字出现 + 实时感）
- 空状态是"什么都没"（vs 引导文案 + 操作入口）
- 进入应用是冷启动（vs 仪式感）

v2.2 一口气把这 6 个维度都做了。结果是 App **打开就有"产品感"**——不需要用户凑近屏幕才能感受到变化。

---

## 关键决策记录

### 字体选型：Noto Serif SC 而非 LXGW WenKai

训练记忆里用户倾向 LXGW WenKai（霞鹜文楷）作为"信札感"字体。但 google_fonts 上**没有**这个字体——LXGW WenKai 是社区字体，不在 Google Fonts 仓库里。两个选择：

1. **打包 LXGW WenKai ttf 到 assets**：5-10MB，启动慢
2. **用 google_fonts 上的 Noto Serif SC**：跨平台一致，启动快（运行时按需下载字符子集）

选 2。理由：v2.2 是"产品级打磨"，不是"字体强迫症"——Noto Serif SC 的衬线感已经能匹配 Lumora"庄重告别"调性。LXGW WenKai 可以放到 v2.3 做"自托管字体优化"。

### 触摸反馈：AnimatedScale 而非 InkWell

Lumora 已有 60 个 `_FloatingParticle`（背景浮动粒子），如果按钮用 InkWell 水波纹，水波纹会跟粒子视觉冲突——看起来像"按钮在吐泡泡"。`AnimatedScale` 更"现代"、更克制，跟暗色夜色调性匹配。

### 路由转场：淡入 + 微缩放，不做左右滑动

平台默认左滑是"工具感"的典型信号。Lumora 宪法第一条是"过渡性客体"——克制、庄重。左滑太"外卖 App"，淡入 + 缩放更"信札"。所有 8 处路由统一改造。

### 打字机：35ms/字（固定值）+ 跳过按钮

中文每分钟约 60-80 字 = 750-1000ms/字 ≈ 75-100ms/字（人眼自然语速）。35ms/字比自然语速略快——配合"信札"调性，更"克制"而非"急躁"。长消息加"跳过"按钮给用户控制权。

### 三态：手写 shimmer 而非用 shimmer 包

shimmer 包是 80 行 Dart 代码的功能。引入新依赖 vs 手写 30 行 AnimatedBuilder——选后者。CLAUDE.md "少依赖"原则。

### 不做 ErrorView

CLAUDE.md 明确说"don't design for hypothetical future requirements"。v2.2 的错误兜底现状（SnackBar）够用，不做 ErrorView。等真有错误兜底需求时再实现。

### 破冰仪式：SpiritScenePage + ChatPage 两段式

plan 里设想 SpiritScenePage 内显示首句 + 打字机——但 SpiritScenePage 没有消息气泡组件，强行加会很复杂。改为两段式：
- **SpiritScenePage**：立绘淡入 800ms + 1.5s 沉默 + 自动进入 ChatPage
- **ChatPage**：`_opening()` 走打字机（v2.2-D）

叙事上是连贯的"立绘醒了 → 自动打招呼 → 开始聊天"，用户体验上是连续的。

---

## 手动测试说明

每个改动都写了 `tool/MANUAL_TEST_v2.2_*.md` 手动测试流程。用户**统一手动测试**而非逐项即时测试。

6 个 manual test 文档：
- `tool/MANUAL_TEST_v2.2_A_FONTS.md`
- `tool/MANUAL_TEST_v2.2_B_PRESSABLE.md`
- `tool/MANUAL_TEST_v2.2_C_ROUTE.md`
- `tool/MANUAL_TEST_v2.2_D_TYPEWRITER.md`
- `tool/MANUAL_TEST_v2.2_E_STATES.md`
- `tool/MANUAL_TEST_v2.2_H_ENTRANCE.md`

每个文档包含：测试步骤、验证清单、已知问题、回退方案。

---

## 已知问题与后续

1. **缩进不一致**：v2.2-B 替换 GestureDetector → Pressable 后，部分组件 children 层级未重新缩进。功能正确但代码可读性下降。下次 `flutter format` 处理。
2. **未实现 SpiritView 强制睁眼**：v2.2-H 破冰仪式没强制睁眼，靠 SpiritView 自带定时器自然眨眼。v2.3 评估加 `forceBlinkOnce` 参数。
3. **GitHub push 失败**：v2.2 期间 GitHub 网络持续连不上，所有 commit 暂未推送到 origin。等网络恢复后 `git push origin main` + `git push origin v2.2.0` + `gh release create`。
4. **v2.3 评估方向**：
   - v3 视觉升级（Live2D/3D 死结仍未解）
   - 阶段判定 + 健康留存度量（宪法第二条落地）
   - 用户偏好开关（关闭破冰 / 关闭自动进入 / 关闭打字机）

---

## 关联文档

- [CHANGELOG.md](./CHANGELOG.md) — 详细变更日志
- [CONSTITUTION.md](./CONSTITUTION.md) — 产品宪法
- [README.md](./README.md) — 项目说明
- `tool/MANUAL_TEST_v2.2_*.md` — 各改动手动测试流程